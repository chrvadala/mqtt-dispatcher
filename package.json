{
  "name": "mqtt-dispatcher",
  "description": "Node.js message dispatcher for MQTT",
  "version": "0.1.0",
  "main": "src/MqttDispatcher.js",
  "repository": "https://github.com/chrvadala/mqtt-dispatcher.git",
  "author": "chrvadala",
  "license": "MIT",
  "scripts": {
    "test:ci": "standard && jest",
    "test": "jest",
    "coveralls": "jest --coverage --coverageReporters=text-lcov | coveralls",
    "standard": "standard",
    "version": "standard && yarn coveralls"
  },
  "engines": {
    "node": ">= 6"
  },
  "keywords": [
    "mqtt"
  ],
  "dependencies": {
    "qlobber": "^3.0.2"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "aedes": "^0.35.0",
    "coveralls": "^3.0.1",
    "jest": "^23.4.2",
    "mqtt": "^2.18.3",
    "portfinder": "^1.0.13",
    "standard": "^11.0.1"
  }
}
